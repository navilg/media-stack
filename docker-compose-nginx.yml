version: '3'
name: nginx
services:
  nginx:
    image: nginx:1.27.1-alpine
    container_name: nginx
    networks:
      - mynetwork
    volumes:
      - /var/www/html:/usr/share/nginx/html
    ports:
      - 80:80
      - 443:443
    restart: always

networks:
  mynetwork:
    external: false
